# 折页使用说明

折页用于把一篇长文章自动整理成多张社交媒体贴图。你可以先自动生成，再在左侧编辑，右侧会实时显示最终效果。

## 一、第一次打开页面

页面默认显示一篇已经排好版的示例。要换成自己的文章，请点击“03 · 排版编辑”右上角的“一键导入”。默认设置如下：

页面右上角有“使用说明”按钮。首次访问时会自动打开三步图解说明；关闭后不会反复打扰，需要时可随时重新打开。

- 配色：白底朱红
- 第一页：标题 + 正文
- 标题字体：思源宋体
- 正文字体：思源宋体
- 图片左下角文案：A股研报局
- 封面署名：A股研报局出品
- 正文页眉：A股研报局 · 行情早知道
- 末页免责声明：开启

右侧预览区显示当前会生成多少张图片。修改左侧内容或设置后，预览会自动重新分页。

## 二、导入文章

### 方法 1：文章链接

1. 在“03 · 排版编辑”右上角点击“一键导入”。
2. 在弹窗中选择“文章链接”。
3. 粘贴公开文章地址。微信公众号文章可直接使用分享链接。
4. 保持“保留原文样式”开启，可尽量保留颜色、强调、边框、卡片和图片。
5. 点击“导入并替换正文”。
6. 等待提示变为“导入完成”。系统会读取正文并自动分页，之后统一在“排版编辑”中修改。

如果链接无法读取，可改用下面的 HTML 或富文本方式。

### 方法 2：HTML

1. 点击“排版编辑”右上角的“一键导入”，选择“HTML”。
2. 粘贴完整 HTML 源码或正文片段。
3. 点击“导入并替换正文”。

### 方法 3：富文本

1. 点击“排版编辑”右上角的“一键导入”，选择“富文本”。
2. 从公众号编辑器、文档或网页复制内容，粘贴到编辑框。
3. 点击“导入并替换正文”。

### 方法 4：Markdown

1. 点击“一键导入”，选择“Markdown”。
2. 粘贴 Markdown 内容。支持标题、粗体、斜体、`==高亮==`、引用、列表和 GFM 表格。
3. 点击“导入并替换正文”。系统会单向转换成富文本。

导入弹窗只用于替换正文，不是第二个长期编辑窗口。导入成功后请关闭弹窗，在页面唯一的“排版编辑”模块继续修改。排版编辑器不再提供 Markdown 往返切换，以避免内容和高亮样式丢失。

## 三、修改标题和品牌文字

在“01 · 封面文案”中可以编辑：

- 醒目标题：每按一次回车就增加一行标题。
- 导语 / 副标题：可留空。留空时，正文会紧接在标题区后面。
- 底部栏目名：显示在独立封面的底部信息区。
- 封面署名：使用独立封面时显示在封面上。
- 正文页眉：显示在每张正文贴图顶部。
- 图片左下角文案：显示在每张正文贴图底部左侧，可独立修改；为空时才回退使用底部栏目名。

建议标题控制在 2～4 行。标题较长时，可在语义停顿处手动换行。

## 四、编辑正文排版

“03 · 排版编辑”里的内容就是当前正文。鼠标悬停任意按钮会出现功能气泡。点击文字或选中内容后，可使用工具栏：

- B：粗体
- I / U / S：斜体、下划线、删除线
- H1 / H2 / H3：三级标题层级
- 引用：生成引用样式
- 字色：打开统一颜色面板，修改选中文字的文字颜色
- 高亮：可在“标记笔”和“实色块”两种固定样式中选择，再设置任意颜色
- 对齐图标：点击后展开左对齐、居中对齐和右对齐三个标准图标
- 无序列表、有序列表和左中右对齐
- 图片：上传 PNG、JPG 或 WebP，并调整宽度、对齐、圆角和图注
- 表格：导入内容中的表格会保留结构和样式，可直接编辑单元格；不提供新建空表格按钮
- ＋前空行：在当前段落前增加可保留的空白
- ＋分页：从光标位置开始新的一页
- ＋领取卡：插入 PDF 刊物领取卡
- 撤销 / 重做：支持按钮以及 `Ctrl+Z`、`Ctrl+Shift+Z`；Mac 可使用 `Command+Z`、`Command+Shift+Z`

粗体、H1/H2/H3、引用和列表都有激活状态；再次点击同一功能即可取消，不需要手动清理 HTML。颜色面板中的“清除颜色”可以只移除当前选择的字色或高亮，不影响其他格式。

### 给部分文字单独改色

1. 在编辑框中选中要强调的文字。
2. 点击工具栏“字色”修改文字前景色；需要强调底色时点击“高亮”，先选择“标记笔”或“实色块”，再选择颜色。
3. 色盘使用相同的最近使用色、推荐色和自定义 HEX 色值。
4. 点击推荐色、最近使用色，或在取色器中自定义颜色。
5. 点击“应用”。右侧预览会同步更新。

颜色面板会记住最近使用的 10 种颜色。需要恢复时，重新选中文字并点击“清除颜色”。

表格较长时，预览会按完整行分页，并在新页重复表头。单元格不会再被拼成连续文字；如果某一行本身包含非常长的内容，可适当调小字号或手动精简该单元格。

### 调整或删除图片

1. 点击工具栏“＋图片”可上传本地 PNG、JPG 或 WebP；导入文章中的图片也可直接编辑。
2. 在编辑框中点击图片。
3. 使用出现的宽度滑杆，或选择 40%、60%、80%、100%。
4. 按需设置靠左、居中、靠右和圆角；输入图注后点击“保存图注”。
5. 点击“删除这张图片”，也可以按 Delete 或 Backspace。

动图或外部平台特殊组件如果无法正常显示，可以直接删除，不影响其他内容。

### 删除横线

1. 点击编辑框中的横线。
2. 点击“删除这条横线”，或按 Delete / Backspace。
3. 删除后会保留一个空行，避免前后文字意外连接。

### 手动分页和留白

- 某一页内容太挤：把光标放在希望换页的位置，点击“＋分页”。
- 某一页空白太多：删除多余分页线，或点击“清除分页线”后重新分页。
- 某段前需要呼吸感：把光标放在该段，点击“＋前空行”。
- 页面底部需要统一留白：调整“页底留白”滑杆。

### 自动优化与排版密度

- “紧凑 / 标准 / 舒展”会同时设置字号、行距和页底留白，选择后仍可继续微调。
- 系统会在内部持续检测页面平衡，但不会在预览或导出图片上显示“页面利用率”标签。
- 正文段落在一页剩余空间不足时会自动跨页续排。切点即使落在加粗、红字、高亮、斜体、下划线或删除线中间，续页也会继承完整行内样式；不会再把整段推到下一页造成大块空白。
- 点击“自动优化分页”会重新检查标题跟随正文、长段落拆分和页面平衡；它不会改写文字，也不会偷偷改变字号或行距。
- 编辑器上方只有一个“一键自动排版”按钮。点击后会按本地规则识别导语、“实话一”等章节标题、“写在最后”等结语、关键数据、重点句、第一/第二等结构段、空段、图片和表格，不需要再选择样式。
- 一键自动排版完全不调用 AI API，不判断观点，也不改写正文。自动结构只作用在右侧展示层，可随时切回“基础样式”；富文本中的手动颜色、强调和原文内容仍会保留。

## 五、选择版式、配色和字体

在“04 · 视觉样式”中：

- “第一页使用标题 + 正文”开启时，大标题约占第一页上半部分，正文从同一页继续。
- 关闭后，会生成一张独立封面，正文从第二张开始。
- 点击 5 张结构缩略图可切换“小红书爆款、财经深度长文、数据指数型、极简新闻型、重点卡片型”。“财经深度长文”适合作为多数 A 股长文的起点。
- 版式和配色互相独立：换版式不会改主题色，换配色也不会改变版式结构。
- 新版式可开启“自动识别文章结构”；右侧顶部可在“美化后”和“基础样式”之间即时比较，原始富文本不会被覆盖。
- 展开当前配色可选择 4 套核心配色、11 套更多配色及已保存的自定义配色。
- 展开“高级设置”后，可分别选择思源宋体或思源黑体，调整排版密度、字号、行距、页底留白和自动分页。
- 在高级设置中点击“打开统一颜色面板”，可在同一弹窗中切换纸张、标题、正文和高亮颜色；编辑器和页面设置共用最近使用颜色。
- 输入方案名并点击“保存当前配色”。下次打开页面仍可在配色面板中点击对应色卡恢复；同名保存会更新原方案。
- 点击“恢复当前版式推荐设置”只恢复字号、行距和留白，不会改变主题色或正文内容。

如果导入文章带有大量原始颜色，关闭“保留原文样式”并重新生成，可让正文更统一地使用当前主题。

## 六、添加 PDF 刊物领取卡

1. 在“02 · PDF 刊物领取卡”填写刊物名称和领取引导语。
2. 上传二维码，支持 PNG、JPG 和 WebP。
3. 在正文编辑框中把光标放到希望插入的位置。
4. 点击“在光标处插入领取卡”。
5. 卡片文案和二维码修改后会实时同步。

二维码需要保持清晰并留有白边。导出后建议用手机实际长按或扫码测试一次。

## 七、风险提示

在“05 · 风险提示”中：

- 可以开启或关闭末页免责声明。
- 提示标题和内容都可自行编辑。
- 风险提示会优先放在最后一页末尾；剩余空间不足时，会完整移到新的一页，避免被截断。
- 输入版本名并点击“保存当前版本”，可以保存多套风险提示；点击已保存名称即可切换。

## 八、选择尺寸并导出

在“06 · 成图尺寸”选择：

- 小红书 3:4：1080 × 1440
- 公众号 4:5：1080 × 1350
- 竖版 9:16：1080 × 1920

预览确认后：

- 点击页面右上角的下载按钮，可导出单张 PNG。
- 点击页面顶部的“批量导出”，会下载包含所有页面的 ZIP 压缩包。
- 下载文件名会自动标注当前平台，例如 `折页-小红书-01.png`、`折页-公众号-01.png` 或 `折页-竖屏9：16-01.png`。
- 页面右上角的圆形下载按钮只用于操作，不会进入导出的图片。

正式发布前，建议逐页检查标题是否完整、图片是否清晰、二维码是否可识别、末页风险提示是否完整。

## 九、浏览器自动保存

正文、标题、品牌文字、版式、配色、字体、尺寸、风险提示和排版参数会自动保存在当前浏览器。关闭网页后再次打开，会恢复上次编辑状态。V4 会自动兼容并迁移旧版工作区数据。

- 自动保存只在同一浏览器、同一站点地址下生效。
- 清除浏览器站点数据或使用无痕模式，会删除已保存内容。
- 超大二维码或超长正文可能超过浏览器容量；出现“无法保存”提示时，请先导出成图或备份正文。

## 十、常见问题

### 点击“导入并替换正文”没有反应

- 确认链接是公开网页，不是后台预览链接。
- 等待页面出现成功或错误提示。
- 微信公众号临时限制访问时，可复制正文改用富文本导入。
- 本地开发时请使用 `npm run dev:pages`；普通 Vite 静态预览不会启动 `/api/import`，因此无法测试链接导入。正式的 Cloudflare Pages 网址不受此限制。

### 导入后只有简介，没有正文

重新点击生成；系统会优先识别公众号的 `#js_content` 正文容器。若原页面结构特殊，使用富文本粘贴最稳定。

### 一张图片占了好几页

在编辑框中点击图片，把宽度调为 40%～80%，或删除不需要的图片。系统会重新计算分页。

### 某页出现大块空白

通常是手动分页、过大的图片或不可拆分的复杂卡片造成。先清除分页线，再缩小图片；仍有问题时可删除该特殊组件。

### 空行保存不住

使用工具栏的“＋前空行”，不要只连续按多次回车。该按钮插入的是排版系统可识别并保留的空行。

### 预览和导出略有差异

先等待字体和图片全部加载，再执行导出。二维码、关键图片和跨页位置应以导出的 PNG 为最终检查对象。

### 再次粘贴富文本后字号突然变大

新版编辑器会先把导入内容标准化，再同步到排版编辑器，避免同一段内联字号被重复换算。请在“一键导入”弹窗的富文本区完成粘贴，再点击“导入并替换正文”，之后继续在“03 · 排版编辑”修改即可。

## 十一、内容使用提醒

请确认你拥有文章、图片、二维码和刊物封面的使用权限。导入第三方网页时，原网页中的动图、交互组件、广告模块或受防盗链保护的图片可能无法完整展示，可以在编辑器中手动删除。
