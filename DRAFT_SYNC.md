# 浏览器草稿同步开发版

工作台的“同步草稿”按“确认内容 → 交给浏览器 → 核对草稿”操作。图片、标题和文案由当前 Chrome 中的草稿助手填入平台原生编辑器，再到平台核对保存结果。本开发版只更新测试环境。

## 使用前提

1. 在 Chrome 扩展商店安装 Tampermonkey，再安装或更新本站提供的折页草稿助手脚本至 0.2.0；更新后刷新折页与平台页面。
2. 用同一个 Chrome 打开折页测试网站和目标平台，并在平台网站自行登录。
3. 打开空白的小红书图文编辑器或公众号原生贴图编辑器。

无需部署同步服务器、下载启动包或运行本机辅助程序。折页不要求平台密码、公众号 AppSecret、同步服务口令或云端二维码。平台登录由平台网站和当前浏览器管理。

每次使用目标平台当前登录的账号。切换账号时，先核对并结束上一组待传内容，再到平台自行切换账号，重新准备下一组内容。

## 操作流程

1. **确认内容**：从当前海报生成真实图片，分别编辑小红书和公众号的标题、文案。检查图片数量和顺序，可添加、替换、移动或删除图片。
2. **交给浏览器**：连接草稿助手，将这组完整素材留在浏览器扩展中。打开平台空白编辑器，在助手面板检查内容后填入；上传过程中保持页面打开。
3. **核对草稿**：在平台检查当前账号、全部图片、顺序、标题和文案，保存为草稿。返回折页查看处理状态，核对平台草稿箱后再确认结果。

“已交给浏览器”只表示素材已交给扩展；“已填入”只表示编辑器已接收素材。人工确认保存后记录为“人工确认”，不把页面提示或模拟测试当作平台保存回执。

已有编辑内容、页面不匹配、上传未完成或结果不确定时，先检查平台页面。助手不会自动重复整组上传或公开发布。结束一组内容会清除扩展中的该组临时素材，不会删除平台草稿。

## 素材与尺寸

只接收 PNG/JPEG，单图不超过 10,000,000 字节，总素材不超过 60 MiB。超限时阻止发送，不静默截断。

| 项目 | 小红书 | 公众号贴图 |
| --- | --- | --- |
| 本工具图片数量上限 | 18 张 | 20 张 |
| 本工具标题上限 | 20 个字符 | 20 个字符 |
| 本工具话题上限 | 10 个 | 10 个 |
| 本工具文案上限 | 1,000 字 | 1,000 字 |
| 模板建议 | 3:4，1080 × 1440 | 4:5，1080 × 1350 |

以上是本工具的保守限制，平台实际响应为准。话题在文案中以 `#` 开头，用空格或换行分隔。比例不符、混用比例或短边不足 720 像素时需核对后继续。同步副本与工作台原稿独立；重新排版后需重新生成同步图片。

## 保存位置与清理

| 资料 | 保存位置 |
| --- | --- |
| 同步图片、两平台文案和确认记录 | 编辑时位于当前页面内存；主动存档、开启自动存档或交给扩展前，会把完整副本写入当前浏览器的 IndexedDB。 |
| 已交给助手的素材与处理状态 | 当前 Chrome 的扩展存储；结束该组内容时清除。 |
| 平台登录状态 | 由平台网站在当前浏览器中管理；折页不建立平台授权包或账号服务器。 |

浏览器存档属于当前网站地址和浏览器，不会自动跨设备同步。删除本机存档不会删除平台草稿、素材或工作台的编辑记录。既有文章链接导入和远程图片代理继续使用 `/api/import` 与 `/api/image`。

## 来源与验证边界

浏览器适配器参考 baoyu-skills 与 OpenCLI 的原生编辑器流程。来源、修改说明及完整许可证位于 [浏览器适配器第三方声明](lib/browserSync/THIRD_PARTY_NOTICES.md)，构建脚本会把相关声明和许可证嵌入发布的用户脚本。

单元回归覆盖扩展消息、来源校验、素材与平台适配；浏览器回归在 GitHub 隔离环境中检查真实 PNG、本地存档、编辑和导出。模拟平台页面通过不等于真实平台草稿保存成功。平台界面变化、登录账号和全部图片是否入稿，仍需在当前实际页面核对。
