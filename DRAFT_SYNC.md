# 平台草稿同步开发版

工作台的“同步草稿”先准备完整原图和两平台独立文案，再分别连接小红书浏览器助手或公众号官方接口，最后在平台核对草稿。本开发版只更新测试分支与预览环境，生产保持原部署。

## 小红书使用前提

1. 在 Chrome 扩展商店安装 Tampermonkey，再安装或更新本站提供的折页草稿助手脚本至 0.3.0；更新后刷新折页与小红书页面。
2. 用同一个 Chrome 打开折页测试网站和目标平台，并在平台网站自行登录。
3. 打开空白的小红书图文编辑器。

小红书无需同步服务器或本机辅助程序。平台登录由平台网站和当前浏览器管理。0.3.0 浏览器助手仅支持小红书，公众号原有 DOM 适配已移除。

每次使用目标平台当前登录的账号。切换账号时，先核对并结束上一组待传内容，再到平台自行切换账号，重新准备下一组内容。

## 公众号使用前提

公众号已确认具备获取调用凭据、上传永久素材、新增草稿、草稿详情等权限。服务使用官方 `newspic` 类型保存“多张海报＋简短配文”，实际访问微信的出口 IP 必须在微信白名单内。当前采用免费本机方案：现有 Cloudflare 测试网页经同域 API 和 Cloudflare Tunnel 连接这台 Mac。同步时需保持电脑开机、联网且不休眠；宽带出口 IP 变化后更新微信白名单。也可把同一服务部署到固定 IP 服务器。

服务端私密配置 AppID、AppSecret、公众号名称和独立连接口令。本机版提供双击配置、启动入口，见 [本机使用说明](server/wechat/本机使用说明.md)。网页只填写独立连接口令，显示配置的目标名称和 AppID 派生标识；口令仅在当前面板内存中，关闭或切换平台后清除。AppSecret 不进入网页、插件或浏览器存档。Quick Tunnel 重启会更换临时地址，需更新 Preview 连接配置；配置细节与部署包见 [公众号部署说明](server/wechat/README.md)。

## 小红书操作流程

1. **确认内容**：从当前海报生成真实图片，分别编辑小红书和公众号的标题、文案。检查图片数量和顺序，可添加、替换、移动或删除图片。
2. **交给浏览器**：连接草稿助手，将这组完整素材留在浏览器扩展中。打开平台空白编辑器，在助手面板检查内容后填入；上传过程中保持页面打开。
3. **核对草稿**：在小红书检查当前账号、全部图片、顺序、标题和文案，保存为草稿。返回折页查看处理状态，核对平台草稿箱后再确认结果。

“已交给浏览器”只表示素材已交给扩展；“已填入”只表示编辑器已接收素材。人工确认保存后记录为“人工确认”，不把页面提示或模拟测试当作平台保存回执。

已有编辑内容、页面不匹配、上传未完成或结果不确定时，先检查平台页面。助手不会自动重复整组上传或公开发布。结束一组内容会清除扩展中的该组临时素材，不会删除平台草稿。

## 公众号操作流程

1. **确认内容**：选择公众号，检查完整海报、顺序及独立标题、短文。
2. **连接公众号**：填写连接口令，检查连接，核对显示的公众号及账号标识。点击“同步到公众号草稿箱”后才上传图片。
3. **核对草稿**：服务上传全部图片后，只创建一篇贴图草稿。读取官方详情核对类型、标题、配文及全部图片素材编号的顺序。接口匹配后仍需打开公众号草稿箱，检查每张图片实际显示，人工确认单独记录。

完整素材和任务编号先存本机再发送。超时、关闭窗口或服务中断后可以读回原任务；同编号不会重新创建。未知结果先人工核对；服务不会自动重试创建或删除已上传素材，也没有群发或发布入口。

## 素材与尺寸

只接收 PNG/JPEG，单图不超过 10,000,000 字节，总素材不超过 60 MiB。超限时阻止发送，不静默截断。

| 项目 | 小红书 | 公众号贴图 |
| --- | --- | --- |
| 本工具图片数量上限 | 18 张 | 20 张 |
| 本工具标题上限 | 20 个字符 | 20 个字符 |
| 本工具话题上限 | 10 个 | 10 个 |
| 本工具文案上限 | 1,000 字 | 1,000 字 |
| 模板建议 | 3:4，1080 × 1440 | 4:5，1080 × 1350 |

公众号文案另有本工具保守的 **2,048 UTF-8 字节**上限，超限提示编辑，不自动截断；不将此称为已核实的平台硬上限。以上限制以平台实际响应为准。话题在文案中以 `#` 开头，用空格或换行分隔。比例不符、混用比例或短边不足 720 像素时需核对后继续。同步副本与工作台原稿独立；重新排版后需重新生成同步图片。

## 保存位置与清理

| 资料 | 保存位置 |
| --- | --- |
| 同步图片、两平台文案和确认记录 | 编辑时位于当前页面内存；主动存档、开启自动存档或同步前，会把完整副本写入当前浏览器的 IndexedDB。 |
| 已交给助手的素材与处理状态 | 当前 Chrome 的扩展存储；结束该组内容时清除。 |
| 小红书登录状态 | 由平台网站在当前浏览器中管理。 |
| 公众号凭据和任务记录 | AppSecret 仅放服务端私密配置；任务编号、内容指纹、素材编号、草稿编号及核对所需文案保存在服务器持久磁盘。原图只在上传期间留于服务内存。 |

浏览器存档属于当前网站地址和浏览器，不会自动跨设备同步。删除本机存档不会删除平台草稿、素材或工作台的编辑记录。既有文章链接导入和远程图片代理继续使用 `/api/import` 与 `/api/image`。

## 来源与验证边界

浏览器适配器参考 baoyu-skills 与 OpenCLI 的原生编辑器流程。来源、修改说明及完整许可证位于 [浏览器适配器第三方声明](lib/browserSync/THIRD_PARTY_NOTICES.md)，构建脚本会把相关声明和许可证嵌入发布的用户脚本。

单元回归覆盖扩展消息、来源校验、素材与平台适配；浏览器回归在 GitHub 隔离环境中检查真实 PNG、本地存档、编辑和导出。模拟平台页面通过不等于真实平台草稿保存成功。平台界面变化、登录账号和全部图片是否入稿，仍需在当前实际页面核对。

0.2.1 按 ProseMirror 段落结构核对正文，保留空段和真实换行；小红书图片预览出现后，继续等待原生预处理与上传状态结束。平台明确报告上传失败时立即停止，保留已确认的图片数量，不自动重试。

当前实机核对中，两组图片上传请求返回 200，保存后也出现了草稿条目；但重新打开草稿，图片仍有 CDN 400/ORB 错误。真实草稿可用性尚未通过，不能把请求成功、预览可见或草稿条目存在当作全部图片已可用的证明。

公众号官方接口、Cloudflare 代理、断线恢复、重复提交与页面分流均有模拟回归，尚未配置真实账号凭据并完成网页到本机的连接。权限截图确认可调用相关接口，不等于贴图草稿真实保存和图片显示已验收。两平台真实验收均只到草稿箱。
