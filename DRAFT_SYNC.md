# 贴图同步开发版

在贴图编辑器中准备完整海报和两平台各自的标题、配文。公众号支持勾选多个账号，提供 **同步到草稿箱** 和 **立即发布** 两个操作；小红书通过本机专用浏览器保存草稿。

当前功能用于开发分支和预览环境，尚未自动部署到正式网站。启动方法见 [Mac 本机使用说明](server/wechat/本机使用说明.md)，连接配置见 [服务说明](server/wechat/README.md)。

## 公众号

1. 启动本机服务，在折页的“连接本机并添加公众号”中填写独立连接口令。
2. 添加公众号名称、AppID 和 AppSecret，点击“保存并连接公众号”。可以添加多个账号，名称由你填写，需自行核对它与 AppID 的对应关系。
3. 检查图片、顺序、标题和配文，勾选本次操作的公众号。
4. **同步到草稿箱**：逐个账号上传图片，保存为一篇 `newspic` 贴图草稿，再读取官方详情核对。完成后打开公众号草稿箱检查图片实际显示。
5. **立即发布**：先核对确认框中的内容和全部目标账号，再确认发布。未保存的内容会先创建草稿；已有同一内容的草稿会复用。服务在发表前再次核对草稿，提交后继续读取微信发表结果。

“微信已接收发表任务”不等于“发表成功”。只有官方状态和文章链接核对通过才显示成功。不同公众号分别处理和保留结果，不保证多个账号同时完成；部分账号失败时，先查看各自记录，不要重做整组内容。

本工具没有文章类型、群发或定时按钮。官方公开发表接口没有定时参数；如需平台托管的定时操作，请在保存草稿后自行到公众号后台处理。折页不在本机倒计时，也不承诺关机后继续执行尚未提交的任务。

## 小红书

小红书使用本机独立的持久浏览器资料，不复用你日常浏览器中的登录。首次操作会打开专用浏览器，由你扫码登录，并在折页核对识别到的账号。

确认海报和文案后，服务在原生编辑器上传图片、填写内容并保存草稿。只有稳定账号标识、草稿标识及重新打开后的标题、配文、全部图片和顺序均核对成功，才记录为已保存。登录未完成、账号无法识别、图片仍在处理或页面结构变化时停止，并保留待核对结果。小红书没有公开发布操作。

这条本机浏览器链路尚未完成真实账号验收。自动化测试和页面预览成功不能替代平台草稿回读；如显示结果待核对，请先在专用浏览器检查原草稿。

## 资料保存在什么地方

| 资料 | 保存位置 |
| --- | --- |
| 公众号 AppSecret、设备连接口令 | 当前浏览器、当前网站地址的独立加密资料库，与贴图存档分开。 |
| 公众号名称、AppID | 同一浏览器资料库，供账号列表和核对使用。 |
| 完整海报、两平台配文、同步记录 | 当前浏览器的贴图存档；编辑中的内容也在页面内存中。 |
| 公众号运行时凭据 | 连接时通过 HTTPS 经过 Cloudflare 同域代理和 Tunnel 到本机服务，仅保留在服务内存中。折页不将其写入云端持久配置、日志或数据库。 |
| 任务及回执 | 本机任务目录，包含核对需要的文案、素材编号、草稿编号和发表任务编号；公众号原图仅在上传期间留于服务内存。 |
| 小红书登录与任务 | 本机专用浏览器资料目录及任务目录。 |

这些资料不会通过折页应用账号跨设备同步。更换浏览器或网站地址、清除网站数据后，需要重新添加公众号；本机服务重启后也需要由浏览器重新连接账号。加密用于避免明文保存，不代表整份浏览器资料无法被复制，也不能防止同源恶意脚本访问。

旧版 `config.env` 可通过“导入本机配置”迁移：文件在浏览器本地解析，不整份上传；其中的账号信息仍会在连接时按上述路径发送。确认迁移及连接成功后，再清理旧文件里的公众号凭据，保留设备口令和任务目录。导入不会自动删除原文件。

## 中断与核对

任务编号会在发送前保存；同一编号只读取已有结果。未知创建或发表结果不会自动重试。已知回执因磁盘故障暂未保存时，页面会提示先恢复存储和核对，不要重启服务或重复提交。删除浏览器存档不会删除平台草稿或素材。

本机和 Tunnel 需要保持运行、联网且不休眠。当前免费 Quick Tunnel 重启会更换临时地址，需要更新预览环境连接地址并重新部署；它不适合作为日常稳定连接。微信白名单应填写本机调用微信的实际公网出口 IP。

## 素材限制

只接收 PNG/JPEG，单图不超过 10,000,000 字节，总图不超过 60 MiB；超限会停止，不自动截断。

| 本工具限制 | 小红书 | 公众号贴图 |
| --- | --- | --- |
| 图片数量 | 18 张 | 20 张 |
| 标题 | 20 个字符 | 20 个字符 |
| 配文 | 1,000 字 | 1,000 字，同时不超过 2,048 UTF-8 字节 |
| 话题 | 10 个 | 10 个 |
| 模板建议 | 3:4，1080 × 1440 | 4:5，1080 × 1350 |

以上为工具限制，实际还需通过平台检查。比例混用、比例不符或短边不足 720 像素时需核对；重新排版后应重新生成同步图片。

## 当前验证结果

- **公众号草稿已完成真实验证**：两张 1080 × 1440 PNG 保存为一篇 `newspic` 草稿；官方素材回读的图片像素与原图一致，用户已在草稿箱看到结果。
- **公众号立即发布尚未真实发表验收**：已用模拟接口验证确认步骤、状态读取、账号隔离、并发、中断和不重复提交。不能把这些测试视为真实发表成功。
- **小红书新本机浏览器链路尚未真实验收**：需要完成当前账号登录、保存和重新打开草稿后的核对。
