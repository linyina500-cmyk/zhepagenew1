# 网页草稿同步开发版

在工作台点击“同步草稿”，按“确认内容 → 选择账号 → 保存结果”操作。可以分别编辑两平台的标题与文案、调整图片，再选择多个账号保存草稿。本开发版只更新测试环境。

## 使用前提

站点管理员需先部署私有同步服务，见 [cloud/DEPLOYMENT.md](cloud/DEPLOYMENT.md)。网页提示“服务尚未部署”时，仍可编辑、导出和保存浏览器存档，但不能连接平台账号。网盘（包括坚果云）不能运行这个服务。

部署完成后，用户在网页填写服务口令即可使用，无需安装软件、下载启动包或保持本机辅助程序运行。服务口令是折页私有服务的访问凭证，与微信或小红书密码无关。

- 勾选“在此浏览器记住账号”：平台授权的加密包保存在当前浏览器；有效期内无需每次重新填写 AppSecret 或扫码。平台主动失效或加密包到期后，需重新连接。服务登录最长 30 天。
- 不勾选：平台授权包只保留在当前页面内存，刷新或关闭页面后需重新连接平台账号。服务 Cookie 为会话 Cookie，服务端有效期最长 8 小时。
- 加密包最长有效 30 天，成功取得新的授权包会更新浏览器中的版本。选择是否记住与连接服务一同生效；已有登录状态可直接恢复。
- 授权包属于当前浏览器、当前网站地址。请使用固定测试地址，切换预览域名、设备或浏览器不会自动带上账号。待核实任务记录始终保存在浏览器中，避免断线后重复发送。

## 连接平台账号

### 公众号贴图

1. 管理员填写账号备注、公众号 AppID 和 AppSecret。
2. 公众号需具备草稿与永久素材接口权限，并将**云端同步服务器的固定公网出口 IP**加入接口白名单。本版使用公众号自己的接口凭证，尚未接入微信第三方平台扫码授权。
3. 服务通过微信官方接口验证账号，再把加密授权包交回浏览器。输入框提交后清空，明文 AppSecret 不保存在网页存档或云端账号文件中。
4. 保存时按顺序上传永久素材，创建 `newspic` 图片草稿，再通过 `draft/get` 核对标题、文案和图片。只有收到有效草稿编号且核对一致，才显示“已保存平台草稿”。

素材上传与草稿创建是分开的操作。上传后创建失败时，素材可能已经留在公众号素材库；本工具不会自动删除。草稿编号不能直接当作网页编辑器地址，所以结果入口指向官方后台。

### 小红书

1. 点击“显示小红书登录二维码”，网页展示临时云端浏览器的登录画面，用手机扫码。可取消登录后重试。
2. 服务核对创作者账号身份，将登录状态加密交回浏览器，然后关闭登录用的临时浏览器。同步时恢复该账号状态，并再次核对身份。
3. 自动填入图片、标题和文案，仅尝试明确的“暂存离开”动作。已有未完成编辑、未知弹窗、上传失败或页面不匹配时停止，不点击发布。
4. 目前没有可稳定验证的远端草稿编号与读回接口，因此暂存后显示“待核实”。请到平台核对后选择“确认已保存”或“确认未保存”；两种确认都不会重新发送。

待核实时，网页可查看临时页面截图，最多保留 10 分钟；确认或到期即关闭浏览器。截图不是可操作的远程桌面，不能处理任意滑块验证、复杂登录挑战或编辑。平台风控、二维码入口变化、云端 IP 可用性，以及暂存内容能否在另一个会话中恢复，均需真实测试账号验收。目前不能把模拟测试通过当成小红书真实同步成功。

待核实的账号会被锁定，后续账号暂停发送。服务重启或请求断线时，浏览器仍保留待核实标识，先到平台核对再解除，避免自动重试生成重复草稿。清空浏览器数据会清掉这些本地保护记录，请先核实未完成任务。

## 素材与尺寸

只接收 PNG/JPEG，单图不超过 10,000,000 字节，总素材不超过 60 MiB。浏览器解码图片，云端再次校验文件结构与真实尺寸；超限时阻止发送，不静默截断。

| 项目 | 小红书 | 公众号贴图 |
| --- | --- | --- |
| 图片数量 | 1–18 张 | 1–20 张 |
| 本工具标题上限 | 20 个字符 | 20 个字符 |
| 本工具话题上限 | 10 个 | 10 个 |
| 本工具文案上限 | 1,000 字 | 1,000 字且 UTF-8 不超过 2,048 字节 |
| 模板建议 | 3:4，1080 × 1440 | 4:5，1080 × 1350 |

话题在文案中以 `#` 开头，用空格或换行分隔，例如 `#旅行 #城市观察`。两平台分别计数。比例是排版建议，比例不符、混用比例或短边不足 720 像素时需核对后继续。原海报和同步副本独立；重新排版后选择“用当前图片新建”更新副本。

## 保存位置与清理

| 资料 | 保存位置与期限 |
| --- | --- |
| 同步图片、两平台文案、账号选择、回执 | 默认当前页面内存。主动“存到本机”或开启自动存档后，保存到当前浏览器 IndexedDB。 |
| 服务登录 | HttpOnly、Secure、SameSite=Strict 的主机限定 Cookie；不放平台凭证。 |
| 平台授权包 | 经过认证加密，由当前浏览器持有。勾选记住才写入 IndexedDB，否则仅页面内存。 |
| 云端明文授权、图片和文案 | 处理期间的进程与临时浏览器内存；不设账号数据库或持久浏览器档案。容器临时目录使用 tmpfs。 |
| 临时云端浏览器 | 登录完成/取消后关闭；同步完成后关闭，待核实时最多保留 10 分钟。 |
| 云端短期结果 | 登录完成结果约 5 分钟；任务回执及更新后的加密包约 24 小时，均为进程内存，重启即失去。定时清理最多延后约 1 分钟。 |
| 未确认任务标识 | 浏览器 IndexedDB 中保存账号和请求编号，不包含明文平台凭证；人工核对后清除。服务运行期间另有内存锁。 |

实现采用 `iron-session` 的认证加密，使用分用途密钥和版本号支持轮换。加密主密钥只在管理员控制的服务端私密文件中；服务只有一名配置的拥有者，不是开放注册的多用户系统。没有独立安全审计，也不能防止已被控制的服务器或浏览器在使用时窃取资料。

“移除账号”删除当前浏览器保存的授权包，不会撤销平台本身的登录或已复制的有效加密包。提前撤销需在平台退出/撤销授权，或由管理员停用对应加密密钥。删除浏览器草稿存档也不会删除平台草稿、素材或工作台自己的编辑记录。

云端会在同步时收到选中账号的授权包、标题、文案和图片，解密后访问相应平台。既有文章链接导入和远程图片代理继续使用原有接口。

## 开源参考

- [iron-session](https://github.com/vvo/iron-session)，MIT：直接使用 `sealData` / `unsealData`，固定 9.0.1，承担认证加密与密钥轮换。
- [Playwright](https://github.com/microsoft/playwright)，Apache-2.0：复用项目现有 1.63.0，用临时浏览器上下文恢复登录状态；容器采用官方固定版本与 seccomp 配置。
- [xiaohongshu-mcp](https://github.com/xpzouying/xiaohongshu-mcp/blob/332d196854a9eac0d2b8c2c0e3d0cc43139d724c/service.go)，Apache-2.0：参考二维码登录会话生命周期，没有复制服务代码或其账号落盘实现。
- [OpenCLI](https://github.com/jackwener/OpenCLI/tree/8271afc67e8504bda94c147f446ee29775d08274)，Apache-2.0：沿用已适配的上传选择器与明确暂存动作，增加身份核对和不确定结果保护。
- [baoyu-skills](https://github.com/JimLiu/baoyu-skills) 与 [Obsidian WeChat Converter](https://github.com/DavidLam-oss/obsidian-wechat-converter)：参考公众号图片草稿工作流，按官方接口独立实现。

许可证与来源见 [cloud/THIRD_PARTY_NOTICES.md](cloud/THIRD_PARTY_NOTICES.md) 和 [THIRD_PARTY_NOTICES_CLOUD.md](THIRD_PARTY_NOTICES_CLOUD.md)。官方接口依据：[公众号新增草稿](https://developers.weixin.qq.com/doc/subscription/api/draftbox/draftmanage/api_draft_add.html)、[永久素材上传](https://developers.weixin.qq.com/doc/service/api/material/permanent/api_addmaterial.html)、[小红书发笔记组件](https://miniapp.xiaohongshu.com/doc/DC530301)。

## 验证范围

单元与 HTTP 测试覆盖加密、失效、账号隔离、网关和来源校验、取消与清理竞态、恢复状态、同步幂等、丢失回执保护、浏览器存储以及两个平台适配器。平台响应使用测试替身。隔离 CI 检查 Chromium、Firefox、macOS WebKit 的界面流程，以及部署容器内临时浏览器的启动、截图、状态恢复和关闭。

这些检查不使用真实平台账号，也不创建真实草稿。云服务器部署、公众号权限与固定出口 IP、小红书真实扫码和跨会话草稿可见性仍需独立验收。
