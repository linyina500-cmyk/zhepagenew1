# 本机草稿同步开发版

编辑完海报后，点击工作台的“同步草稿”，可以分别编辑小红书和公众号的标题、文案，添加、替换、删除或调整图片顺序，再选择一个或多个账号保存草稿。首图作为封面；两平台共用同一组图片。功能在独立开发分支验证，不更新正式站。

## 启动与使用

Mac 启动包适用于 **macOS 14 或更新版本**，不需要输入命令或复制配对码。

1. 在预览页面下载 Mac 本机启动包，先解压整个文件夹。根目录只有启动文件、说明和“程序文件”文件夹。
2. 双击其中的 **“启动折页.command”**。不要只把启动文件移到其他地方。
3. 如果尚未安装 Node.js，启动窗口会打开[官方下载页](https://nodejs.org/zh-cn/download)。选择 Node.js 24 或更新版本的 macOS 安装包，完成图形安装后再次双击即可。
4. 启动器会自动准备依赖和排版字体。已有 Google Chrome 时使用现成浏览器程序，否则按需下载浏览器组件；小红书始终使用独立登录资料。首次可能花几分钟，窗口只显示中文进度，无需重复操作。
5. 页面自动打开并连接本机助手后，编辑海报，点击“同步草稿”，依次完成“确认内容”“选择账号”“保存结果”。小红书在独立窗口扫码；公众号由管理员完成首次连接。

使用期间保持启动窗口开启。刷新或关闭网页后，再次双击同一个启动文件，会复用已经运行的助手并重新打开已连接的页面。关闭启动窗口会停止本次助手，不会删除草稿或账号资料。

如果下载的启动文件被 Mac 阻止打开，可右键选择“打开”并按系统提示确认；仍受阻时保留提示截图联系开发者，无需关闭系统安全保护。如果提示端口被占用，请先关闭之前手动启动的折页页面和助手窗口，再次双击。启动器不会终止其他程序。失败时窗口会显示可读提示和本机日志位置。

公开预览可以准备图片和文案；实际账号同步请使用启动包打开的本机页面。两者的浏览器存档相互独立，在线内容不会自动转移，请在本机版导入原文或图片后继续。首次使用建议先启动本机版，再开始编辑。Windows 目前只有网页版编辑功能，尚未提供可双击使用的同步启动包。

双击入口固定使用 `http://127.0.0.1:5173`，助手只监听 `127.0.0.1:47831`，并且只接受该页面来源。若浏览器询问本地网络访问权限，允许这个本机页面连接即可。解压包不含账号资料或登录凭据；字体下载会逐项核对大小与 SHA-256，已有匹配文件不会重复下载。

开发者仍可使用项目原有的 `draft:dev` 和 `draft:helper` 启动命令单独调试。运行环境需要 Node.js 22.18+（22.x）或 24+，见 [Node.js 官方说明](https://nodejs.org/api/typescript.html)。

### 公众号贴图

1. 填写账号备注、公众号 AppID 和 AppSecret。“账号备注”由用户填写，核对账号以已验证的 AppID 为准。
2. 对应公众号需具备草稿与永久素材接口权限，按公众平台设置把本机当前公网出口 IP 加入接口白名单。开发版采用公众号自己的接口凭证，尚未接入第三方平台扫码授权。
3. 选择账号后同步。助手按图片顺序上传永久素材，调用官方 `draft/add` 创建 `newspic` 图片消息，再通过 `draft/get` 核对标题、文案和图片顺序。只有取得有效草稿编号并核对一致才显示“已保存平台草稿”。
4. 打开公众号后台查看草稿。草稿接口的 `media_id` 不能直接当成网页编辑器的 `appmsgid`，因此入口指向官方后台首页。

AppSecret 提交后会清空输入框，仅在助手进程内存中保留。重启助手后，用相同 AppID 重新验证即可恢复原账号；未确认任务的锁定状态仍保留。上传素材和创建草稿是分开的官方操作：如果上传后创建失败，素材可能已经留在公众号素材库中，本工具不会自动删除它们。

### 小红书

1. 点击“打开小红书登录窗口”，在独立窗口内完成扫码登录。等待时可以继续编辑，也可以点“取消登录”后重试；直接关闭扫码窗口会结束本次等待。账号备注可以不填；每个账号有独立会话，不读取用户平时浏览器的登录资料。
2. 助手从当前创作者页面核对真实账号标识，并在提交前再次核对；身份不一致会停止操作。
3. 图片、标题和文案会填入图文编辑器，仅尝试明确的草稿保存动作。已有未完成编辑、未知弹窗、上传失败或页面结构不匹配时，停止并保留窗口供用户检查。
4. 当前没有取得可稳定验证的远端草稿编号与读回接口，因此执行保存动作后仍显示“待核实”。请在该账号的创作者页面核对标题和图片，再选择“确认已保存”或“确认未保存”。这两种确认只记录核对结果、解除账号锁定，不会再次发送。

小红书使用网页适配，平台页面变化可能使其失效；本次开发未使用真实账号完成平台端验收。实现不点击发布，也没有静默后退、关闭窗口等推测保存路径。待核实期间禁止重试或移除该账号，助手重启后也保留限制，避免重复生成草稿。人工确认已保存会单独标为“用户确认已保存”，不会冒充自动验证成功。

## 素材与尺寸检查

本工具只接收 PNG/JPEG，单图不超过 10,000,000 字节，总素材不超过 60 MiB。文件会在浏览器解码，在助手端校验文件结构和真实尺寸；超过数量或内容上限时，先修改再同步，不静默截断。

| 项目 | 小红书 | 公众号贴图 |
| --- | --- | --- |
| 图片数量 | 1–18 张 | 1–20 张 |
| 本工具标题上限 | 20 个字符 | 20 个字符 |
| 本工具话题上限 | 10 个 | 10 个 |
| 本工具文案上限 | 1,000 字 | 1,000 字且 UTF-8 不超过 2,048 字节 |
| 本站模板建议 | 3:4，1080 × 1440 | 4:5，1080 × 1350 |

话题填写在文案中，以 `#` 开头，用空格或换行分隔，例如 `#旅行 #城市观察`。两个平台分别计数，最多 10 个；页面会显示实时计数，超出时不能保存到平台草稿。

模板比例是排版建议，不是平台强制尺寸。比例不符、混用比例或图片短边不足 720 像素时，会提示用户返回工作台调整，或检查后明确选择沿用。改动图片后需重新核对提示。原始海报和草稿副本相互独立；在工作台重新排版后，需选择“用当前图片新建”更新草稿图片。

小红书数量与文案限制参考官方发笔记组件；网页入口的实际限制仍以平台响应为准。公众号文案的官方字段说明有多个长度表述，开发版采用上述保守上限，界面按约 680 个汉字提示，表情和标点也占用长度。

## 本机资料与清理

| 资料 | 保存位置与生命周期 |
| --- | --- |
| 平台草稿图片、两平台文案、账号选择、同步回执 | 默认仅在当前窗口内存。点击“存到本机”或主动开启自动存档后，保存一份到当前浏览器 IndexedDB。可删除存档。 |
| 配对信息 | 双击启动时自动交给本机页面，页面立即清除地址片段；仅保留在页面和助手内存，不写入网页存储、终端或启动日志。助手重启后失效。 |
| 公众号 AppSecret、访问令牌 | 仅助手进程内存；只发往微信官方 API。不会写入账号文件、浏览器存档或错误回执。 |
| 账号备注、平台账号标识、未确认任务标识 | 助手账号目录中的 `accounts.json`，不含正文、图片、AppSecret 或访问令牌。 |
| 小红书登录会话 | 助手独立浏览器档案 `profile-<账号编号>`，仅在本机。移除账号会关闭对应窗口并删除档案。 |

助手目录：macOS 为 `~/Library/Application Support/Zhepage Draft Helper`；Windows 为 `%LOCALAPPDATA%\Zhepage Draft Helper`；Linux 为 `~/.local/share/Zhepage Draft Helper`。macOS/Linux 目录权限为当前用户可访问、账号文件为当前用户可读写；这不是独立加密保险箱，小红书会话仍受设备本身的账户和磁盘保护约束。

移除账号时，如果登录档案清理失败，账号会保留以便重试；若后续账号记录保存失败，登录资料可能已经清理，仍可再次移除。

页面与助手之间使用来源校验、目标地址校验和本次启动的配对码；拒绝任意 URL、外部 Cookie 导入和公网访问。本机草稿不会发往折页服务器。实际点击同步时，所选标题、文案和图片会交给相应平台；原有的文章链接导入、远程图片代理仍使用项目现有接口，不能把整个应用理解为离线工具。

删除“本机存档”只删除当前浏览器保存的这份平台草稿，不删除平台草稿、平台素材或助手账号。原有工作台编辑记录仍使用它自己的本地保存机制。

## 开源来源与核查依据

2026-09-09 核查了项目星数、许可证和实际保存路径：

| 项目 | 核查时星数 / 许可证 | 本次采用方式 |
| --- | --- | --- |
| [OpenCLI](https://github.com/jackwener/OpenCLI) | 29,125 / Apache-2.0 | 移植小红书上传选择器与明确草稿动作，固定参考提交 `8271afc67e8504bda94c147f446ee29775d08274`，增加账号核对、已有编辑保护及不确定结果锁定。 |
| [baoyu-skills](https://github.com/JimLiu/baoyu-skills) | 25,770 / MIT | 核查官方公众号图片草稿与永久素材接口模式；本项目按官方接口独立实现。 |
| [Obsidian WeChat Converter](https://github.com/DavidLam-oss/obsidian-wechat-converter) | 307 / MIT | 核查多账号、顺序图片与 `newspic` 工作流；未复制源代码。 |
| [Wechatsync](https://github.com/wechatsync/Wechatsync) | 6,286 / GPL-3.0 | 核查后未移植：网页接口不等于已验证的官方贴图 API，小红书部分也不能从公开代码确认。 |

改编来源与完整 Apache 许可证见 [companion/THIRD_PARTY_NOTICES.md](companion/THIRD_PARTY_NOTICES.md)。

官方依据：[公众号新增草稿](https://developers.weixin.qq.com/doc/subscription/api/draftbox/draftmanage/api_draft_add.html)、[永久素材上传](https://developers.weixin.qq.com/doc/service/api/material/permanent/api_addmaterial.html)、[小红书发笔记组件](https://miniapp.xiaohongshu.com/doc/DC530301)。

## 验证范围

`npm test` 包括本机档案、素材验证、来源与配对校验、启动包内容、字体核验、重复启动、账号隔离、取消登录及其并发边界、幂等处理和两个平台适配器回归；平台响应由测试替身提供。浏览器用例覆盖三步导航、素材生成与排序、本机恢复、尺寸提醒、多账号回执、人工核对、异常响应、移动布局、线上模式引导，以及通过真实 HTTP 连接测试助手的完整界面流程。自动连接分别验证首次带口令打开页面和口令稍后到达已打开页面，两种方式都不得将口令写入网页存档。扫码等待、取消失败和重新连接时会验证编辑仍可继续，未确认结果不得被当作成功。

隔离 CI 分别运行 Chromium、Firefox 与 macOS WebKit。测试助手只使用合成凭证和模拟平台响应，不登录真实平台，不创建真实草稿。真实公众号权限、平台响应及小红书页面保存表现仍需用用户自己的测试账号验收；这些未验证部分不能由模拟用例通过代替。
