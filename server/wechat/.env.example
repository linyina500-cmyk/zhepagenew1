# 本机服务只持久保存独立连接口令，不保存公众号 AppSecret。
# 在折页网页添加公众号，凭据只在该浏览器加密保存，连接时进入本机服务内存。
# 至少32位随机口令，供折页网页连接本服务；不是公众号AppSecret。
WECHAT_SYNC_TOKEN=
# 持久磁盘目录，重启和升级时必须保留，不能使用临时容器目录。
WECHAT_DATA_DIR=/var/lib/zhepage-wechat
WECHAT_HOST=127.0.0.1
WECHAT_PORT=8788
